pipeline {
  agent any
  stages {
    stage('Init') {
      steps {
        echo 'Init'
      }
    }
    stage('Build') {
      steps {
        echo 'Compile'
      }
    }
    stage('Deploy') {
      steps {
        echo 'Deploy'
      }
    }
  }
}